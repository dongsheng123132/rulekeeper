# 规则怪谈·无限层 —— 共创参与指南

> 一部规则可以被机器验证的小说。违反规则 = 被门禁拒稿，也 = 故事里的抹除。
> 本仓库是共创入口：提交层设定、抓规则 bug、投票决定剧情走向。

## 快速参与（五个入口，从易到难）

### ① 投票（零门槛，每周一次）
GitHub Discussions 每周开「候选层投票」：3 个候选层，Reactions 点赞（👍），
得票最高者下周写进正文。你的一票决定剧情。

### ② 提名（一句话参与）
开 Issue 选 `层设定提名` 模板，一句话描述你想看的场景：
「我想看一个深夜食堂的层」「我想看废弃游乐场的层」。
官方收集整理，进候选池。

### ③ 创作：提交层设定（深度参与）
按 `层设定` Issue 模板提交完整设定（层名 + 3-7 条规则 + 怪物/物品 + 隐藏联系），
审核合入 spec 后，下一章就可能写你设计的层，正文永久署名：
「层设计者：@你的GitHub」。

**层设定要求**：
- 规则 3-7 条，必须可机器判定（明规则：行为+后果；暗规则：触发条件）
- 提供规则关键词表（正文违禁词，用于机器校验）
- 不要与已建层冲突（先看 spec/ 目录）

### ④ 抓虫：找规则的 bug（本仓库特色）
规则是对象、是机器可验证的。检查任意章节正文是否违反已建立的规则
（尤其是层01·便利店的「只收现金/不数人头/不看第七排」），
发现违反行为就开 Issue 选 `规则虫` 模板报告。
首个有效报告者进贡献榜头条。别的小说没法抓虫，这部可以。

### ⑤ 番外（进阶创作）
写守规人出任务间隙的小故事，符合 canon（不违反已建立规则/状态）的
合入仓库，署名进 provenance。

## 贡献荣誉闭环
- 所有合入贡献记录在 provenance（by 字段 = 你的 GitHub id）
- README 贡献榜每月更新
- 被写进正文的层永久署名

## 规则
- 正文由 AI 经本象协议门禁生成，人工策展世界观与规则
- 规则校验当前为词表级（text_not_contains），句子级语义校验在迭代计划中
- 提交前先读 CONTRIBUTING 全篇 + spec/ 目录了解已建世界

## 目录结构
```
spec/           世界规格（canon/ 人物物品层 / narrative/ 伏笔 / tasks/ 禁区规则）
chapters/       正文（ch01.txt, ch02.txt …，每章一个文件）
state.jsonl     世界状态流（每个 commit 的语义事务，公开可查）
```

## IP 声明
IP 权属：贺去病（hequbing.com）。正文、世界观、角色、层设定 canon 版权归作者。
共创者贡献按贡献授权条款进入 canon（贡献 = 授权项目使用），保留署名权与个人展示权。
